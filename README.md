# swift-dsa
Swift algorithms with tests done in XCTest

Ignore the swiftUI files. Running XCTest in a barebones IOS app template is easier than running it on Playground. Also code structuring is better.

Start by referring to the `swiftDSA/algorithms` folder.
